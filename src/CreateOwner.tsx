import { Link } from "react-router-dom";
import PetOwnerForm from "./PetOwnerForm";

const CreateOwner = () => {
  return (
    <div>
      CreateOwner
      {/*<PetOwnerForm />*/}
    </div>
  );
};

export default CreateOwner;
